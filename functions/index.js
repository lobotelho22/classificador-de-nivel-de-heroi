const { calculadoraDePartidas } = require('./calculadora-de-partidas');
const { criarHeroi } = require('./classes-do-jogo');
const { classificador } = require('./classificador-de-nivel');

module.exports = {
    calculadoraDePartidas,
    criarHeroi,
    classificador,
}
